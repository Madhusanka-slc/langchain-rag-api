---

# LangChain RAG API

A lightweight **Retrieval-Augmented Generation (RAG) API** built with **LangChain and FastAPI**.  
It enables chat with **private knowledge sources** using vector embeddings and semantic search.

---